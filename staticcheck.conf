# Staticcheck configuration for GOTRS
# Temporarily disable unused code checks to unblock build while legacy handlers are integrated.
# TODO: Re-enable U1000 after removing or wiring all legacy handlers.
checks = [
  "all",
  "-U1000",
]
