# external_ticket_rules.yaml example
# Define regex-based partner follow-up rules. Each rule must include at least one search target
# (subject, body, or headers) and the regex must provide a capturing group with the GOTRS ticket
# number. Rules are evaluated in file order.

rules:
  - name: vendor_case_subject
    pattern: "Case #([0-9]{10})"
    search_subject: true

  - name: vendor_case_body
    pattern: "TicketID=([0-9]{10})"
    search_body: true

  - name: vendor_header
    pattern: "Partner-([0-9]+)"
    headers:
      - X-Partner-Ticket
