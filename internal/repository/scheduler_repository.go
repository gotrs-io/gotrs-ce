package repository

// SchedulerRepository is intentionally left empty. The new scheduler runtime
// operates against in-memory job definitions and existing OTRS tables, so the
// dedicated repository from the earlier design is no longer required.
