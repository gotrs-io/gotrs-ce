package api

// RegisterRoutingHandlers registers all handlers with the routing system
// NOTE: This function is disabled to avoid circular dependency
// The routing registration should be handled at the application level
func RegisterRoutingHandlers() {
	// Handler registration moved to application level to avoid circular dependency
}

// init function runs automatically when the package is imported
func init() {
	// Routing registration disabled to avoid circular dependency
}