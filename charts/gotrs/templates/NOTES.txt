GOTRS has been installed!

{{- if .Values.ingress.enabled }}

Access GOTRS at:
{{- range .Values.ingress.hosts }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}
{{- end }}

{{- else }}

Get the application URL by running:
  export POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "gotrs.name" . }},app.kubernetes.io/instance={{ .Release.Name }},app.kubernetes.io/component=frontend" -o jsonpath="{.items[0].metadata.name}")
  kubectl --namespace {{ .Release.Namespace }} port-forward $POD_NAME 8080:80

  Then open http://localhost:8080 in your browser.

{{- end }}

Database: {{ .Values.database.type }}
{{- if .Values.database.external.enabled }}
  Host: {{ .Values.database.external.host }}
{{- else }}
  Host: {{ include "gotrs.database.host" . }}
{{- end }}

{{- if or .Values.valkey.enabled .Values.externalValkey.enabled }}

Cache: Valkey
  Host: {{ include "gotrs.valkey.host" . }}
{{- end }}

For more information, visit: https://gotrs.io/docs
